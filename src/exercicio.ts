//função de multiplicação
function multiplicar(numero1: number, numero2: number ): number {
    return numero1 * numero2;
}

console.log(multiplicar(2, 4));

// função de saudação

function dizOla(nome: string): string {
    return `Olá ${nome}`;
}

console.log(dizOla('Anderson'));